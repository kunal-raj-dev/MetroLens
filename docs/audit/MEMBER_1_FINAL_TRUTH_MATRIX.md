# MEMBER 1 — FINAL TRUTH MATRIX

**Subsystem**: Member 1 — Multilingual OCR Engine & Service  
**Auditor**: Independent Principal Engineer  
**Date**: 2026-09-05  
**Hierarchy**: ACTUAL CODE > INDEPENDENT TEST EXECUTION > INDEPENDENT EXPERIMENT > RAW ARTIFACT > CURRENT STATE > DOCUMENTATION > HISTORICAL REPORT  

---

| Claim | Actual Evidence | Test Evidence | Benchmark Evidence | Status | Confidence | Notes |
| :--- | :--- | :--- | :--- | :--- | :--- | :--- |
| **OCR Exists** | Fully implemented in `packages/ocr/src/nirikshak_ocr` (12 modules: detector, recognizer, router, engine, service, etc.) | `test_ocr_smoke.py`, `test_ocr_engine_comprehensive.py` passing | `benchmarks/ocr/final/run_final_benchmark.py` executes successfully | **VERIFIED** | HIGH | Native direct implementation |
| **OCR Runs End-to-End** | Pipeline runs: Image -> Resize -> DBNet++ -> Quad Crop -> Router -> SVTR -> Reading Order | 108 tests passing across test suite | Profiling across 4 specimens over 20 iterations each completed | **VERIFIED** | HIGH | Executed live on AMD Ryzen host |
| **Models Correct** | Models are DBNet++ mobile (`ch_PP-OCRv3_det_infer.onnx`), SVTR-EN (`ch_PP-OCRv3_rec_infer.onnx`), SVTR-HI (`rec.onnx`), Hindi dict (`dict.txt`) | Verified in `test_devanagari_dictionary_dimension_alignment` and `test_validate_manifest_blocked_and_populated_states` | SHA-256 hashes match `models/manifest.yaml` byte-for-byte (100% match) | **VERIFIED** | HIGH | Provenance traced to PaddleOCR / monkt Devanagari port (Apache-2.0) |
| **Runtime Correct** | Direct `onnxruntime==1.29.0` with `CPUExecutionProvider` (intra-op: 4, inter-op: 1). Zero RapidOCR/Paddle runtime in inference. | Verified active provider via `session.get_providers() == ['CPUExecutionProvider']` | Benchmark executed with `CPUExecutionProvider` | **VERIFIED** | HIGH | RapidOCR wrapper completely eliminated from production path |
| **Offline Operation** | 100% local weights, local dictionary, local execution. Zero network calls or runtime downloads. | `test_ocr_strictly_offline` and `test_offline_execution_socket_guard` pass with monkeypatched socket blocker | Zero socket attempts recorded across all benchmark runs | **VERIFIED** | HIGH | Edge air-gapped readiness verified |
| **Unicode & Currency** | Preserves Devanagari script (Unicode range 0x0900–0x097F) and Indian Rupee symbol (`₹`) without Mojibake. | `test_unicode_utf8_devanagari_serialization_roundtrip`, `test_unicode_nfc_and_currency_preservation` passing | Specimen `SYNTH-02-HIN-FMCG` and `SYNTH-03` decode Hindi characters and `₹` | **VERIFIED** | HIGH | NFC normalization in evaluation module |
| **Coordinates & Geometry** | 4-point quadrilateral ordered clockwise `[tl, tr, br, bl]` in original unnormalized image pixel space; derived bbox strictly encloses polygon. | `test_coordinate_invariance_and_bounding_envelope`, `test_polygon_geometry_contract_and_ordering` passing | Coordinates unscaled via `remap_polygon_to_original` and verified numerically | **VERIFIED** | HIGH | All coordinates strictly bounded in `[0, W]` and `[0, H]` |
| **Script Routing** | Latin crops route to SVTR-EN; Devanagari crops route to SVTR-HI via heuristic confidence gate. | `test_extract_bilingual_mixed_script`, `test_compute_routing_accuracy_independent_of_cer` passing | Routing counts recorded in `OCRResult.routing_summary` across bilingual specimens | **VERIFIED** | HIGH | Heuristic gate; NOT a deep neural script classifier |
| **Fallback Logic** | Ambiguous or low-confidence English prediction triggers Devanagari evaluation fallback. | `test_routing_and_fallback.py` passes: noise crop and Devanagari trigger fallback branch | `fallback_used` flag observable in tokens / router output | **VERIFIED** | HIGH | Fallback margin: +0.15 threshold |
| **Preprocessing Defaults** | Default preprocessing is `B0_BASELINE_RAW` (`preprocessing_mode="raw"`). Adaptive is optional/experimental. | `test_default_config_is_raw_baseline`, `test_service_initialization_defaults_to_raw` passing | `compare_preprocessing.py`: RAW achieves 111.0 ms vs 126.2 ms for ADAPTIVE with identical token yield (37 vs 37) | **VERIFIED** | HIGH | RAW default enforced; Adaptive adds +13.7% latency overhead |
| **Service Layer** | `OCRService` provides singleton lifecycle (`get_instance()`), input normalization (bytes, path, ndarray), caller memory protection, and serialization. | `test_ocr_service_integration.py` (17 tests passing), `test_service_singleton_vs_fresh_instance` passing | Benchmarked across file path, raw binary bytes, and canonical observations | **VERIFIED** | HIGH | Caller input array immutability verified |
| **API Readiness** | Member 4 FastAPI endpoint (`/api/v1/inspect`) consumes `OCRService` via `InspectionPipelineWorker`. | `apps/api/tests/test_api_smoke.py` (4 tests passing) | Cold service load = 350.02 ms; Warmup = 15.55 ms | **VERIFIED** | HIGH | Live HTTP multipart upload returns 200 OK |
| **Real-Data Validation** | Real-world physical retail packaging evaluation. | Checked disk: exactly 0 physical images. All 8 images are synthetic under `data/synthetic/regression/`. | Zero real-world accuracy claims fabricated. | **PENDING** | HIGH | Formally marked PENDING / NOT VERIFIED (Path B active; assigned to Member 6) |
| **Performance Claims** | Latency is reproducible on host CPU. (Note: 200ms and 2.5s are engineering targets, not legal requirements). | `test_ocr_chunk3_regression.py` passes; engine median: ~124–167 ms; service observation median: ~152–185 ms. | Machine-generated in `benchmarks/ocr/final/results.json` | **VERIFIED** | HIGH | Median and P95 independently measured over 20 iterations each |
| **Memory Bounded** | Memory does not experience unbounded growth during repeated inference cycles. | `test_memory_and_concurrency.py`: RSS increases from ~116 MB to ~189 MB during initial pool allocation, then delta is +0.48 MB over 70 concurrent calls. | Recorded in `benchmarks/ocr/final/results.json` | **VERIFIED** | HIGH | Honest description: memory stabilizes at ~190–400 MB depending on execution run depth |
| **Concurrency Safety** | Multi-threaded callers execute without memory corruption or race conditions. | `test_concurrency_thread_safety` (8 threads, 0 errors, identical token counts) passing | Concurrency sweep across 1, 2, 4, 8 workers completed with 100% token accuracy | **VERIFIED** | HIGH | Execution is thread-safe via serialization lock; throughput does not scale linearly on CPU |
| **Reproducibility** | Fresh agent can install, verify models, run tests, and run benchmark. | Commands verified: `python -m pytest` runs 108 tests; `run_final_benchmark.py` runs end-to-end. | Complete commands documented in `MEMBER_1_REPRODUCIBILITY.md` | **VERIFIED** | HIGH | Zero `sys.path` hacks required |
| **Seam Boundary Isolation** | Zero legal metrology rules, physical mm units, or semantic parsing inside Member 1. | Grep search confirmed zero legal logic; `test_no_semantic_or_legal_contamination_in_tokens` passes. | Output contract emits raw pixel geometry only | **VERIFIED** | HIGH | Strict compliance with architectural boundaries |
