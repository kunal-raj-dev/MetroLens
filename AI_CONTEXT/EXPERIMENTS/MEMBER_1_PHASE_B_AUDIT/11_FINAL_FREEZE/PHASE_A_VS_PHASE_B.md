# PHASE A CLAIMS VS PHASE B EVIDENCE

**Auditor**: Independent Principal Engineer  
**Date**: 2026-09-05  
**Subsystem**: Member 1 — Core Multilingual OCR Subsystem  

| Phase A Claim | Phase B Evidence | Same? | Corrected? | Status | Explanation |
| :--- | :--- | :--- | :--- | :--- | :--- |
| **Model Hashes & Paths**<br>`environment.json` claimed models were in `models/` with SHA-256 hashes starting with `3439588c27cf...`, `897a3ede72ea...` | Model files reside under `models/weights/ocr/det/` and `models/weights/ocr/rec_*/`. SHA-256 hashes computed on disk: `3439588c030faea3...` (det), `897a3ededb38fee0...` (rec_en), `43df175fa3c877fb...` (rec_hi), `b5f1be6d8bbff1a1...` (dict) | NO | YES | CORRECTED | Phase A wrote truncated/hallucinated hash suffixes and paths in `environment.json`. Phase B corrected `environment.json` with exact disk paths and byte-verified SHA-256 hashes. |
| **Devanagari Recognizer Vocabulary Alignment**<br>Claimed 100% complete SVTR-HI Devanagari recognition without flaws. | `rec.onnx` has output dimension 169 classes. `CTCLabelDecoder` only loaded 168 classes because `dict.txt` had space on line 1, skipping trailing space. Index 168 was silently dropped. | NO | YES | CORRECTED (BUG-001) | Identified BUG-001. Fixed in `SVTRRecognizer` and `CTCLabelDecoder` by dynamically padding trailing space when `expected_classes` exceeds loaded lines. Verified 169/169 alignment. |
| **Test Suite Count**<br>Phase A claimed 100 / 101 tests passing. | Independent execution ran full test suite: 101 passed. Added 7 independent Phase B audit tests, yielding 108 tests passing (100% pass rate). | NO | YES | VERIFIED (108 Tests) | Verified all 101 existing tests plus added 7 independent audit tests in `tests/unit/test_ocr_phase_b_independent_audit.py` to prevent self-validation. Total: 108 tests. |
| **Real Packaging Data Status**<br>Documentation claims varied from "real packaging validation complete" to "pending". | Examined repository: exactly 8 synthetic images found under `data/synthetic/regression/`. Physical retail package count is 0 (ZERO). | YES | NO | VERIFIED (PENDING) | Disclosed unequivocally: Real-data validation is PENDING / NOT VERIFIED (Path B active). Zero real-world accuracy claims fabricated. |
| **Zero RapidOCR in Production Inference**<br>Claimed RapidOCR is completely eliminated from inference path. | Audited all production imports in `nirikshak_ocr`: only `onnxruntime`, `cv2`, `numpy`, `pyclipper`, `shapely`, `pydantic`. RapidOCR is in `pip list` (site-packages) but completely unimported by `nirikshak_ocr`. | YES | NO | VERIFIED | Core inference is 100% native ONNX Runtime without RapidOCR dependency. |
| **Strict Offline Operation**<br>Claimed 100% offline edge execution. | Tested via socket connection blocker monkeypatch; zero network calls made. Zero remote downloads. | YES | NO | VERIFIED | Confirmed 100% offline. Models are local; runtime is local. |
| **Preprocessing Default**<br>Claimed B0_BASELINE_RAW (`preprocessing_mode="raw"`) is default. | Verified `OCRConfig.preprocessing_mode == "raw"`. Benchmarked RAW vs ADAPTIVE across 8 specimens: RAW achieved 111.0 ms average latency vs 126.2 ms for ADAPTIVE with identical token yield (37 vs 37). | YES | NO | VERIFIED | RAW default confirmed. Adaptive adds +13.7% latency overhead without token yield benefit on pristine images. |
| **Memory Leak Behavior**<br>Phase A claimed "zero memory leak". | Measured memory: Initial: 70.6 MB, Post-Init: 108.9 MB, Warmup: 116.0 MB, After 40 cycles: 189.3 MB, After 70 concurrent cycles: 189.8 MB. | NO | YES | CLARIFIED | Memory pool stabilizes at ~190 MB after initial ONNX Runtime workspace buffer allocation. Delta over subsequent 70 calls is +0.48 MB (bounded). Honest statement: memory is bounded, not "zero leak". |
| **Concurrency Scalability**<br>Phase A claimed thread-safe concurrency. | Tested 2, 4, 8 concurrent threads. All completed with 0 errors and identical token counts. However, execution is serialized by `_engine_lock`. | NO | YES | CLARIFIED | Concurrency is thread-safe via serialization lock, not parallel CPU scaling. Latency scales with thread count due to CPU contention. Corrected documentation. |
| **Seam Boundary Isolation**<br>Claimed zero legal metrology rules, physical mm units, or semantic parsing in M1. | Grep search for "MRP", "USP", "Rule", "mm", "calibration" across `nirikshak_ocr`: all occurrences are strictly in docstrings/comments demarcating boundaries. Zero legal logic. | YES | NO | VERIFIED | Member 1 boundary strictly preserved: Raw image -> OCR -> Geometry & Transcribed Tokens. |
| **Path Portability**<br>Claimed engine resolves models from any directory. | Executed from repo root, `apps/api/`, `tests/`, and external temporary directories. `config.py` walks up tree to locate `models/` successfully in all cases. | YES | NO | VERIFIED | Path portability 100% verified. |
