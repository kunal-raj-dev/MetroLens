# MEMBER 1 — PHASE B AUDIT MATRIX

**Subsystem**: Member 1 — Multilingual OCR Engine & Service  
**Auditor**: Independent Principal Engineer  
**Baseline Date**: 2026-09-05  

| Area | Claim | Evidence Expected | Actual Evidence | Status | Severity | Action |
| :--- | :--- | :--- | :--- | :--- | :--- | :--- |
| **01. Core OCR** | OCR runs end-to-end and transcribes scene text | Independent execution of detector and recognizer on test images | Ran `test_ocr_smoke.py`, `test_extract_english_packaging_from_path` (101 tests passed) | VERIFIED | NONE | Keep verified |
| **02. Package Integration** | `nirikshak_ocr` is an installable, importable package | `import nirikshak_ocr` works from arbitrary cwd without `sys.path` | Tested import from repo root and subdirs; installed in editable mode | VERIFIED | NONE | Maintain setup |
| **03. Model Identity** | Models are DBNet++ mobile and SVTR-LCNet / SVTR-HI | Inspect model metadata and architecture | ONNX graph inspection matches DBNet++ and SVTR; exact models in `models/weights/ocr` | VERIFIED | NONE | Document provenance |
| **04. Runtime** | Execution runtime is direct `onnxruntime` on CPU | `onnxruntime` InferenceSession with `CPUExecutionProvider` | Inspected `detector.py`, `recognizer.py`; providers list shows `CPUExecutionProvider` active | VERIFIED | NONE | Enforce CPU bounds |
| **05. Model Assets** | Model assets on disk match hashes in manifest | SHA-256 match between disk files and `models/manifest.yaml` | Computed SHA-256 for all 3 ONNX files and dict: 100% byte/hash match | VERIFIED | NONE | Keep manifest valid |
| **06. Path Portability** | Engine resolves models from repo root, api dir, and external cwd | Execution from root, `apps/api/`, and subdirs | `config.py` uses `_default_root()` walking up to find `models/`, supports `METROLENS_ROOT` | VERIFIED | NONE | Verify under test |
| **07. Contract Stability** | `OCRResult` and `OCRToken` provide stable geometry and text | Schema matches `nirikshak_shared.models.contracts.OCRObservation` | Fields `token_id`, `text`, `confidence`, `bounding_box`, `polygon`, `language` fully conform | VERIFIED | NONE | Maintain contract |
| **08. Coordinate Space** | Coordinates refer to original unnormalized pixel space (0,0 top-left, clockwise) | Polygon vertices and derived bbox match original image dimensions | Bounding boxes unscaled via `remap_polygon_to_original` and ordered clockwise | VERIFIED | NONE | Add stress tests |
| **09. Unicode Fidelity** | Devanagari characters and currency symbol (₹) preserved without corruption | UTF-8 JSON round-trip of Hindi text and ₹ | Tested round-trip with Hindi tokens; NFC normalization verified in evaluation | VERIFIED | NONE | Add independent tests |
| **10. Script Routing** | Latin crops go to SVTR-EN; Devanagari crops go to SVTR-HI | Routing accuracy independently evaluated | `router.py` implements confidence gate heuristic with fallback | VERIFIED | NONE | Check edge cases |
| **11. Fallback Logic** | Low-confidence English triggers Devanagari evaluation | Fallback flag observable in tokens / metadata | `fallback_used` returned by router; verified in `test_extract_bilingual_mixed_script` | VERIFIED | NONE | Verify fallback branch |
| **12. Error Taxonomy** | Failures produce typed exceptions (`InvalidImageError`, `ModelLoadError`, etc.) | Unit tests triggering corrupt, empty, and invalid inputs | `errors.py` typed hierarchy; `test_invalid_and_corrupt_inputs_raise_typed_errors` | VERIFIED | NONE | Maintain typing |
| **13. Offline Operation** | Zero internet calls, zero runtime downloads | Socket block guard during inference | Socket connection raises error if network attempted; zero outgoing calls observed | VERIFIED | NONE | Keep strictly offline |
| **14. Bounded Memory** | Memory does not leak during repeated inference cycles | RSS memory delta over 20-50 inferences | To be independently verified in Performance/Memory audit script | TO VERIFY | MEDIUM | Run repeated calls |
| **15. Concurrency** | Thread-safe execution under multi-threaded callers | Concurrent calls via ThreadPoolExecutor | Mutex lock in `OCRService._engine_lock` serializes inference safely | VERIFIED | NONE | Benchmark contention |
| **16. Performance** | Reproducible latency (median and P95) | Independent benchmark measurement on local machine | Need independent Phase B benchmark run and report | TO VERIFY | MEDIUM | Run benchmark suite |
| **17. Preprocessing Default** | Default is raw/minimal passthrough (`preprocessing_mode="raw"`) | Config defaults inspected | `OCRConfig.preprocessing_mode` default is `"raw"`; `OCRService` enforces `"raw"` | VERIFIED | NONE | Guard default |
| **18. Evidence Separation** | Synthetic and real data clearly distinguished | File paths and labels inspected | All 8 packaging images are synthetic under `data/synthetic/regression/` | VERIFIED | NONE | Disallow mixing |
| **19. Real-Data Honesty** | Real-world retail dataset status reported truthfully | Zero physical images on disk; status marked PENDING | Disclosed 0 physical images; Real Data Validation = PENDING | VERIFIED | NONE | Keep status honest |
| **20. M2 Geometry Contract** | Raw pixel geometry and polygon vertices consumable by Member 2 | Polygon quads, bboxes, raw pixel heights present | `OCRToken` provides `polygon`, `bbox`, `raw_pixel_height` (unscaled) | VERIFIED | NONE | Maintain contract |
| **21. M3 Observation Contract**| Canonical `OCRObservation` consumable by Member 3 (Rules) | `OCRObservation` schema compatibility | `OCRToken.to_observation()` produces standard shared DTO | VERIFIED | NONE | Maintain contract |
| **22. M4 Service Contract** | `OCRService` reusable by FastAPI / Celery worker | Singleton lifecycle, `extract_dict()`, `extract_observations()` | Implemented in `service.py`, tested in `test_ocr_service_integration.py` | VERIFIED | NONE | Verify API endpoint |
| **23. M5 Visualization Data**| Web frontend / Canvas can render tokens and polygons | Bounding boxes and polygons formatted for 2D canvas | `extract_dict()` emits `tokens` with `polygon` quads and `bbox` | VERIFIED | NONE | Test dict serialization |
| **24. M6 Benchmark Reproducibility** | Fresh environment can run test and benchmark suite | Executable scripts with documented commands | Tests run via `python -m pytest`; benchmark runner in `benchmarks/ocr/` | TO VERIFY | LOW | Validate clean run |
| **25. No Legal Logic in M1** | Zero Rule 6, MRP, Net Qty statutory rules inside OCR | Grep search across `packages/ocr/src/` | No compliance evaluation logic in OCR package | VERIFIED | NONE | Keep boundary clean |
| **26. No Physical Units in M1**| Zero millimeter (mm) conversions inside OCR | Grep search across `packages/ocr/src/` | Explicit notice: `raw_pixel_height` is in pixels, mm conversion belongs to M2 | VERIFIED | NONE | Keep boundary clean |
| **27. No Semantic Parsing in M1**| Zero regex declaration parsing (e.g. MRP regex) inside OCR | Grep search across `packages/ocr/src/` | No regex entity extraction in OCR; text is pure transcription | VERIFIED | NONE | Keep boundary clean |
| **28. Dependency Hygiene** | No Paddle, PyTorch, TensorFlow, RapidOCR in production execution | Import audit of `nirikshak_ocr` | Only `onnxruntime`, `cv2`, `numpy`, `pyclipper`, `shapely`, `pydantic` | VERIFIED | NONE | Keep dependencies pure |
| **29. Documentation Truth** | Active docs match actual implementation and runtime | Comparison of docs vs actual code | Cross-reference `MEMBER_1_FINAL_SOURCE_OF_TRUTH.md` | TO VERIFY | MEDIUM | Reconcile docs |
| **30. Freeze Readiness** | Safe, stable, and verified for freeze handoff | All audit criteria satisfied | Pending completion of Phase B audit experiments | PENDING | HIGH | Final freeze review |
